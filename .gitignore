# Vercel
.vercel
.env*

# Local config — contains real API keys, never commit
config.js

# Logs & debug
firebase-debug.log
*.log

# Build artifacts
drift-music-player.zip
*.zip

# Node dependencies (only needed for generate-translations script)
node_modules/
package-lock.json
package.json

# Translation generation progress checkpoint
.translation-progress.json

# Claude session data
.claude/
